---
apiVersion: v1
kind: ConfigMap
metadata:
  name: rocketmq-operator-config
  namespace: refuel-cat
  labels:
    app: rocketmq-operator
    release: "0.1.3"
data:
  operator-config.yaml: |
    #namespace: refuel-cat
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rocketmq-operator
  namespace: refuel-cat
  labels:
    release: "0.1.3"
    app: rocketmq-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rocketmq-operator
  template:
    metadata:
      annotations:
        tke.cloud.tencent.com/networks: tke-route-eni
      labels:
        app: rocketmq-operator
    spec:
      serviceAccountName: rocketmq-operator
      nodeSelector:
        rocketmq-operator: "on"
      volumes:
        - name: rocketmq-operator-config-volume
          configMap:
            name: rocketmq-operator-config
      containers:
      - name: rocketmq-operator-controller
        imagePullPolicy: Always
        image: yeehawmars/rocketmq-operator:0.1.3
        ports:
        - containerPort: 10254
        volumeMounts:
        - name: rocketmq-operator-config-volume
          mountPath: /etc/rocketmq-operator
        args:
          - --v=4
          - --namespace=rocketmq-operator
